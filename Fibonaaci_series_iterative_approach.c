#include<stdio.h>
//iterative approach
int main(){
    int number,a=0,b=1,c;
    printf("Enter The Numbers");
    scanf("%d",&number);
    if(number==0)(
        printf("%d",number)
    );
    
    else{  printf(" 0 ");
            for(int i=1;i<number;i++){
                a;b;
                c=a+b;
                
                a=b;
                b=c;
                printf("  %d  ",c);

            };
        
    };
}